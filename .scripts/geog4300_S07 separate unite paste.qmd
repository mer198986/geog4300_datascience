---
title: "Data wrangling: more helpful functions"
output: html_notebook
editor_options: 
  chunk_output_type: console
---

```{r}
#| label: setup
#| include: false
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
library(tidyverse)
```

## Using paste, separate, and unite to tidy data

A common part of data wrangling is combining or splitting variables for use in analysis. Two powerhouse functions for this purpose are separate and unite--both of them are from the `tidyr` package, which is part of the tidyverse. As their names, suggest, the first splits columns and the second combines them.

Let's start with the Waffle House dataset from Safegraph, a point-of-interest provider who aggregates GPS location data from multiple cellphone companies.

```{r}
#| label: load-data
whdata <- read_csv("data/whdata_dec21_mar22.csv")
```

### Splitting columns with `separate`

Right now, the dataset includes the address of each store in one column. However, sometimes we want to separate the house number from the name of the street. To do so, we need a separator. This can include some kind of punctuation (e.g., comma, dash, period), but even just a space can do. Here's how you could use that with separate:

```{r}
whdata_1<-whdata %>%
  separate(street_address,into=c("house_num","street"),sep=" ")
```

Looking at this data file allows you to see that this function worked, but you also got an error saying that much of the address was discarded! That's because there's more than one space. The `extra` parameter allows us to select what to do with that extra data. In this case, we'll merge it with everything else in the street column.

```{r}
whdata_2<-whdata %>%
  separate(street_address,into=c("house_num","street"),sep=" ",extra="merge")
```

Parsing further (e.g., pulling out road type like "St.", "Rd.") would be more complex, but `separate()` handles the basic parsing well.

### Combining columns with `unite`

To *unite* data, we can simply list the variables we want to combine together. So to join address, city, and state, we could do the following. This code creates a new variable called `fulladd` that unites the three address variables.

```{r}
whdata_3<-whdata %>%
  unite(fulladd,c(street_address,city,region),sep = ", ") #concatenation of multiple existing string/text into one value. Underscore is the default
#Paste is more like excel's concatenate
```

Note that this removes all the component columns by default. We can use the `remove` parameter to keep them.

```{r}
whdata_4<-whdata %>%
  unite(fulladd,c(street_address,city,region),sep = ", ",remove=FALSE)
```

### Creating text with `paste` and `paste0`

Lastly, `paste` is an all purpose function similar to unite that's used to concatenate data. Using paste allows you more flexibility in how the columns are combined. See this code for an example that creates a text description of each store's location. Note that `paste0` is a variation of this function that doesn't insert a space between the different elements.

```{r}
whdata_5<-whdata %>%
  mutate(descript=paste0(location_name," is located at ",street_address,
                        " which is in the city of ",city,", ",region,"."))

#paste adds a space in between the things, Paste0 does not automatically add a space in between the things. A way to tie text together
```

#####You try it!
The `bucketed_dwell_times` variable lists how many minutes people stayed at Waffle House. Use `separate` to divide this variable into distinct columns.

```{r}
whdata_bucket<-whdata %>%
  separate(bucketed_dwell_times,into=c("TUnder5","T5_10","T11_20","T21_60","T61_120","T121_240","TOver240"),sep=",") %>%
  separate(TUnder5,into=c("AA","TUnder5"),sep=":") %>%
  select(-AA) %>%
  separate(T5_10,into=c("BB","T5_10"),sep=":") %>%
  select(-BB) %>%
  separate(T11_20,into=c("CC","T11_20"),sep=":") %>%
  select(-CC) %>%
  separate(T21_60,into=c("DD","T21_60"),sep=":") %>%
  select(-DD) %>%
  separate(T61_120,into=c("EE","T61_120"),sep=":") %>%
  select(-EE) %>%
  separate(T121_240,into=c("FF","T121_240"),sep=":") %>%
  select(-FF) %>%
  separate(TOver240,into=c("GG","TOver240"),sep=":") %>%
  select(-GG) %>%
  separate(TOver240,into=c("TOver240","GG"),sep="}") %>%
  select(-GG) 
```

Look through the list of functions in the `tidyr` package. You can either search for its documentation online or look it up in the "Packages" tab in the lower left panel of Rstudio. What are one or two other functions that you think could be useful?

## Further text cleaning

Sometimes in data wrangling you have to clean up text after you parse/organize it. For example, the "visits_by_day" field provides a list of the number of registered visits for each day of the weekly time period. What if we wanted to separate those into separate columns? We could use this code:

```{r}
whdata_6<-whdata %>%
  select(placekey:region,visits_by_day) %>%
  separate(visits_by_day,into=c("D1","D2","D3","D4","D5","D6","D7"),sep=",") 
```

Inspecting the data shows that `D1` and `D7` are problematic, though--they have square brackets from the original data field. We can use the `str_detect` function from the tidyverse (technically the `stringr` package) to erase those. The code below uses mutate, replacing the square brackets with a null character. Because those brackets are used in coding, we have to tell R that we mean the character version by adding two backslashes beforehand: `\\[` rather than just `]`.

```{r}
whdata_6<-whdata %>%
  separate(visits_by_day,into=c("D1","D2","D3","D4","D5","D6","D7"),sep=",") %>%
  mutate(D1=str_replace(D1,'\\[',''),
         D7=str_replace(D7,'\\]',''))
```

The visit count columns (D1–D7) are currently character type (since they came from a string). Convert them to numeric using `across`:

```{r}
#| label: convert-numeric
whdata_6a <- whdata_6 %>%
  mutate(across(c(D1:D7), as.numeric))
```

#####You try it!
Do more work with the `bucketed_dwell_times` variable to tidy those data. More specifically, you should have variable columns named after the dwell time categories (<5, 5-10, 11-20, etc.) and the count of users for each category listed under each one. Remove any excess characters that aren't meaningful.

If you want a further challenge, try to identify what other chains Waffle House patrons most commonly visit on the same day (`related_same_day_brand`)

```{r}

```

###Long and wide format
Summarizing multiple variables can be tricky. You can calculate each mean separately, but this can be cumbersome for a large number of variables. One solution is to use functions that collapse multiple variables down to just two columns: one for the variable name and the other for the observational values. This "long" format is useful in lots of ways, but for our purposes, it can allow us to summarize multiple variables in just one line of code. The two commands we use for this in the tidyverse are pivot_wide and pivot_longer.

### From wide to long
Let's say we wanted to look at visits per day for different waffle houses. We could first select just the variables of interest and then use 'pivot_longer' to bring all these variables into two *longer* columns. The *names_to* parameter names the variable with all the former variable headings. The *values_to* parameter tells what to do with all the values in those columns. Here's what this looks like.

```{r}
wh_visits_long<-whdata_6a %>%
  select(placekey,location_name,street_address,city,region,D1:D7) %>%
  pivot_longer(D1:D7,
               names_to="day",
               values_to="visits")
```

You'll notice that this new table has just seven columns, and that the day and visits are now in two long columns. Now let's get the mean visits per day and location.

```{r}
wh_visits_long1<-wh_visits_long %>%
  group_by(placekey,location_name,street_address,city,region,day) %>%
  summarise(mean_visit=mean(visits))
```


### From long back to wide
The resulting table is hard to read. We can now use 'pivot_wider' to push out the data *wide*. In this case, we just have to identify the variable with the new variable names (using 'names_from') and values (using 'values_from').

```{r}
wh_visits_long2<-wh_visits_long1 %>%
  pivot_wider(names_from=day,
              values_from=mean_visit)
```

## You try it!
Use pivots, group_by, and summarise to get the mean visits per day per state.

```{r}

```

